# Optional — the app already defaults to the live prod backend (see app.json
# `extra`). Override here only to point at a local server.
# EXPO_PUBLIC_API_URL=http://192.168.1.10:5100/api/v1
# EXPO_PUBLIC_SOCKET_URL=http://192.168.1.10:5100
